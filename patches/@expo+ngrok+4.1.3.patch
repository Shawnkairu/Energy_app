diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..bb1e9a1 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -28,18 +28,29 @@ class NgrokClient {
       }
     } catch (error) {
       let clientError;
+      const res = error.response;
+      const rawBody =
+        res && typeof res.body === "string"
+          ? res.body
+          : res && res.body != null
+            ? String(res.body)
+            : "";
       try {
-        const response = JSON.parse(error.response.body);
-        clientError = new NgrokClientError(
-          response.msg,
-          error.response,
-          response
-        );
+        if (rawBody) {
+          const response = JSON.parse(rawBody);
+          clientError = new NgrokClientError(
+            response.msg || error.message,
+            res,
+            response
+          );
+        } else {
+          throw new Error("empty body");
+        }
       } catch (e) {
         clientError = new NgrokClientError(
-          error.response.body,
-          error.response,
-          error.response.body
+          error.message || "Ngrok request failed",
+          res,
+          rawBody || undefined
         );
       }
       throw clientError;
@@ -52,8 +63,22 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
-      throw new NgrokClientError(response.msg, error.response, response);
+      const res = error.response;
+      const rawBody =
+        res && typeof res.body === "string"
+          ? res.body
+          : res && res.body != null
+            ? String(res.body)
+            : "";
+      let parsed;
+      try {
+        parsed = rawBody ? JSON.parse(rawBody) : null;
+      } catch {
+        parsed = null;
+      }
+      const msg =
+        parsed && parsed.msg ? parsed.msg : error.message || "Ngrok request failed";
+      throw new NgrokClientError(msg, res, parsed);
     }
   }
 
