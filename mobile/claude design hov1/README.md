# Archived design prototypes (deprecated)

This folder contains **historical Claude design exports** from before the canonical `docs/imported-specs/` model.

Do **not** treat copy or flows here as product truth. Prefer:

- `docs/imported-specs/` — canonical scenarios and installation/DRS/LBRS specs
- `packages/shared` — formulas and gates
- `mobile/components/` and `website/src/` — shipping UI

Known stale patterns in this folder: common-bus apartment model, legacy `installer` technical naming, 80% DRS approve, generated-solar payout language.
