import { officialPalette } from "@emappa/ui";

/** Resident role foreground/background for demo chrome (matches ROLE_TINT.resident). */
export const ROLE_TINT = {
  fg: officialPalette.foxOrange,
  bg: "#FFF4E8",
} as const;
